## ~ 以外の要素を選択する

```css
.manual-btn:not(:last-child) {
  margin-right: 40px;
}
```

* class 属性が `.manual-btn` となっている要素の `最後の要素` を除いて、margin-right を設定する

参考
https://developer.mozilla.org/ja/docs/Web/CSS/:not